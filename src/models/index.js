const User = require('./users.model');
const Trip = require('./trip.model'); 
const Story = require('./story.model');
const WishList = require('./wishList.mode');
const BucketList = require('./bucketList.model')

module.exports = {
    User,
    Trip,
    Story,
    WishList,
    BucketList
}